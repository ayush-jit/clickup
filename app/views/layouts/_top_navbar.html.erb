<span class="glyphicon glyphicon-home"></span> <%= link_to "Home", root_path %>
<span class="glyphicon glyphicon-list-alt"></span> <%= link_to "projects", projects_path %>
<ul class="nav navbar-nav navbar-right">
  <ul class="nav navbar-nav">
    <div class="dropdown">
      <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">Options
      <span class="caret"></span></button>
      <ul class="dropdown-menu">
        <% if user_signed_in? %>
          <li><%= link_to "Profile", home_show_path %></li>
          <li><%= link_to "Sign-out", destroy_user_session_path, data: {confirm: "Are you sure you want to log out?", method: :destroy} %></li>
        <% else %>
          <li><%= link_to "Sign_up", new_user_registration_path %></li>
          <li><%= link_to "Login", new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </ul>
</ul>
